export enum Role {
  ADMINISTRATOR = 'Administrator',
  CENSOR = 'Censor',
  LECTURER = 'Lecturer',
}

export enum Gender {
  Male,
  Female,
}

export enum UserStatus {
  DE_ACTIVE = 'de_active',
  ACTIVE = 'active',
}

export const MAX_PROJECT_INSTRUCTOR = 10;
